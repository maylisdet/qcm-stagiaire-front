import * as React from 'react';
import Button from '@mui/material/Button';

const LogoutButton = () => {
  return (
    <Button variant="outlined" href='/login'>Logout</Button>
  )
}

export {LogoutButton}